services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - .:/app

  # db:
  #   image: postgres:16
  #   environment:
  #     POSTGRES_USER: triage
  #     POSTGRES_PASSWORD: triage
  #     POSTGRES_DB: triage_db
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data

# volumes:
#   pgdata: